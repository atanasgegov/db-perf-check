spring:
  profiles: 
    active: mongodb # elasticsearch,mongodb,cassandra
 

logging:
  level:
    root: INFO
    com.akg: INFO

commons:
  input-data-file: src/main/resources/winemag-data_first150k.csv
  batch-size: 5000
  frequency-output-in-ms: 10000
  use-cases:
    # possible values are: one, crud
    active-use-case: crud
    # Execution type possible values: 
    #   what: elasticsearch, mongodb 
    #   mode: inserts, search, updates, deletes
    one: {what: cassandra, mode: search, time-in-ms: 10000}  
    crud:
      - {what: mongodb, mode: inserts, time-in-ms: 10000} 
      - {what: mongodb, mode: search, time-in-ms: 10000} 
      - {what: mongodb, mode: updates, time-in-ms: 10000}
      - {what: mongodb, mode: deletes, time-in-ms: 10000}
